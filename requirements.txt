# WS-2.6 merged: P3 framework-comparison notebook runtime + tjs_sensitivity simulation deps
# Exact pins (==) for notebook-runtime packages preserve P3's existing convention.
# Range pins (>=,<) for simulation packages preserve the zip's convention from
# tjs-sensitivity-simulation/requirements.txt (Walter Phase 6.2).
# Lock file (requirements.lock.txt) regenerated downstream from this merged baseline.

# Notebook runtime (P3 existing — exact pins)
pandas==2.3.3
jupyterlab==4.5.6
nbformat==5.10.4
nbclient==0.10.4
jupyter_client==8.8.0
ipykernel==7.2.0
nbconvert==7.17.1

# Simulation code dependencies (added at WS-2.6 from tjs-sensitivity-simulation)
numpy>=1.24,<3
matplotlib>=3.7,<4
pytest>=7.0,<9
